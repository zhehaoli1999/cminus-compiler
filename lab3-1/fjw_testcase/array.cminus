int gcd (int u, int v) {
    if (v == 0) return u;
    else return gcd(v, u - u / v * v);
}
void main(void) {
    int emptyArray[10];
    int x; int y; int temp;
    x = 72;
    y = 18;
temp=emptyArray[y-x];
    if (x<y) {
        temp = x;
        x = y;
        y = temp;
    }
    output(gcd(x,y));
    return;
}
